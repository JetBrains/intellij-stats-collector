/*
 * Copyright 2000-2017 JetBrains s.r.o.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 * http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */

package com.completion.ranker.models.gbt_19_25_04;

public class MLWhiteBox {
    public double makePredict(double... features) {
        double result = 0.0523781666603;
        result += new Tree0().calcTree(features) * 0.05;
        result += new Tree1().calcTree(features) * 0.05;
        result += new Tree2().calcTree(features) * 0.05;
        result += new Tree3().calcTree(features) * 0.05;
        result += new Tree4().calcTree(features) * 0.05;
        result += new Tree5().calcTree(features) * 0.05;
        result += new Tree6().calcTree(features) * 0.05;
        result += new Tree7().calcTree(features) * 0.05;
        result += new Tree8().calcTree(features) * 0.05;
        result += new Tree9().calcTree(features) * 0.05;
        result += new Tree10().calcTree(features) * 0.05;
        result += new Tree11().calcTree(features) * 0.05;
        result += new Tree12().calcTree(features) * 0.05;
        result += new Tree13().calcTree(features) * 0.05;
        result += new Tree14().calcTree(features) * 0.05;
        result += new Tree15().calcTree(features) * 0.05;
        result += new Tree16().calcTree(features) * 0.05;
        result += new Tree17().calcTree(features) * 0.05;
        result += new Tree18().calcTree(features) * 0.05;
        result += new Tree19().calcTree(features) * 0.05;
        result += new Tree20().calcTree(features) * 0.05;
        result += new Tree21().calcTree(features) * 0.05;
        result += new Tree22().calcTree(features) * 0.05;
        result += new Tree23().calcTree(features) * 0.05;
        result += new Tree24().calcTree(features) * 0.05;
        result += new Tree25().calcTree(features) * 0.05;
        result += new Tree26().calcTree(features) * 0.05;
        result += new Tree27().calcTree(features) * 0.05;
        result += new Tree28().calcTree(features) * 0.05;
        result += new Tree29().calcTree(features) * 0.05;
        result += new Tree30().calcTree(features) * 0.05;
        result += new Tree31().calcTree(features) * 0.05;
        result += new Tree32().calcTree(features) * 0.05;
        result += new Tree33().calcTree(features) * 0.05;
        result += new Tree34().calcTree(features) * 0.05;
        result += new Tree35().calcTree(features) * 0.05;
        result += new Tree36().calcTree(features) * 0.05;
        result += new Tree37().calcTree(features) * 0.05;
        result += new Tree38().calcTree(features) * 0.05;
        result += new Tree39().calcTree(features) * 0.05;
        result += new Tree40().calcTree(features) * 0.05;
        result += new Tree41().calcTree(features) * 0.05;
        result += new Tree42().calcTree(features) * 0.05;
        result += new Tree43().calcTree(features) * 0.05;
        result += new Tree44().calcTree(features) * 0.05;
        result += new Tree45().calcTree(features) * 0.05;
        result += new Tree46().calcTree(features) * 0.05;
        result += new Tree47().calcTree(features) * 0.05;
        result += new Tree48().calcTree(features) * 0.05;
        result += new Tree49().calcTree(features) * 0.05;
        result += new Tree50().calcTree(features) * 0.05;
        result += new Tree51().calcTree(features) * 0.05;
        result += new Tree52().calcTree(features) * 0.05;
        result += new Tree53().calcTree(features) * 0.05;
        result += new Tree54().calcTree(features) * 0.05;
        result += new Tree55().calcTree(features) * 0.05;
        result += new Tree56().calcTree(features) * 0.05;
        result += new Tree57().calcTree(features) * 0.05;
        result += new Tree58().calcTree(features) * 0.05;
        result += new Tree59().calcTree(features) * 0.05;
        result += new Tree60().calcTree(features) * 0.05;
        result += new Tree61().calcTree(features) * 0.05;
        result += new Tree62().calcTree(features) * 0.05;
        result += new Tree63().calcTree(features) * 0.05;
        result += new Tree64().calcTree(features) * 0.05;
        result += new Tree65().calcTree(features) * 0.05;
        result += new Tree66().calcTree(features) * 0.05;
        result += new Tree67().calcTree(features) * 0.05;
        result += new Tree68().calcTree(features) * 0.05;
        result += new Tree69().calcTree(features) * 0.05;
        result += new Tree70().calcTree(features) * 0.05;
        result += new Tree71().calcTree(features) * 0.05;
        result += new Tree72().calcTree(features) * 0.05;
        result += new Tree73().calcTree(features) * 0.05;
        result += new Tree74().calcTree(features) * 0.05;
        result += new Tree75().calcTree(features) * 0.05;
        result += new Tree76().calcTree(features) * 0.05;
        result += new Tree77().calcTree(features) * 0.05;
        result += new Tree78().calcTree(features) * 0.05;
        result += new Tree79().calcTree(features) * 0.05;
        result += new Tree80().calcTree(features) * 0.05;
        result += new Tree81().calcTree(features) * 0.05;
        result += new Tree82().calcTree(features) * 0.05;
        result += new Tree83().calcTree(features) * 0.05;
        result += new Tree84().calcTree(features) * 0.05;
        result += new Tree85().calcTree(features) * 0.05;
        result += new Tree86().calcTree(features) * 0.05;
        result += new Tree87().calcTree(features) * 0.05;
        result += new Tree88().calcTree(features) * 0.05;
        result += new Tree89().calcTree(features) * 0.05;
        result += new Tree90().calcTree(features) * 0.05;
        result += new Tree91().calcTree(features) * 0.05;
        result += new Tree92().calcTree(features) * 0.05;
        result += new Tree93().calcTree(features) * 0.05;
        result += new Tree94().calcTree(features) * 0.05;
        result += new Tree95().calcTree(features) * 0.05;
        result += new Tree96().calcTree(features) * 0.05;
        result += new Tree97().calcTree(features) * 0.05;
        result += new Tree98().calcTree(features) * 0.05;
        result += new Tree99().calcTree(features) * 0.05;
        return result;
    }
}
